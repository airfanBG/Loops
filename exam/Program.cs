using System;

namespace exam
{
    class Program
    {
        static void Main()
        {
            int hour = int.Parse(Console.ReadLine());
            int minutes = int.Parse(Console.ReadLine());
            int examENDHour = 0;
            int examENDminutes = 0;
            string resultHours = examENDHour.ToString();
            string resultMinutes = examENDminutes.ToString();

                

        }
    }
}
